node_modules/
.next/
out/
coverage/
playwright-report/
test-results/
*.tsbuildinfo
next-env.d.ts

# Secrets — never commit
.env
.env.local
.env.*.local

# Local object storage (development documents)
/storage/*
!/storage/.gitkeep
/backups/

src/generated/
.DS_Store
Thumbs.db

# Integration-test file storage
/storage-test/
/backups-test/

# Playwright output
test-results/
playwright-report/

# Bundled server tools (built in the Docker image / CI)
/dist/

# Per-environment secret files (templates stay tracked)
.env.*
!.env.example
